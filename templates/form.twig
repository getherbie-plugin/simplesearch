<div class="plugin-simplesearch-form">
    <form action="{{ url_rel(route) }}" method="get">
        <input type="text" name="query" value="{{ query }}">
        <input type="submit" value="{{ translate('simplesearch', 'search') }}">
    </form>
</div>
